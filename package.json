{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "backend": "yarn --cwd json-server start",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@liene-putnina/eslint-config-lintmyride": "^1.3.0",
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "nuxt": "^3.5.2",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "install": "^0.13.0"
  }
}
